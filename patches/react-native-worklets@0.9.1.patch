diff --git a/Common/cpp/worklets/SharedItems/Serializable.h b/Common/cpp/worklets/SharedItems/Serializable.h
index 41940974a549a2a2b7fc1117e4de79ddfc7283a0..94fb350a4ac4bd1410d3b25f002312318c6b7253 100644
--- a/Common/cpp/worklets/SharedItems/Serializable.h
+++ b/Common/cpp/worklets/SharedItems/Serializable.h
@@ -39,7 +39,7 @@ template <typename BaseClass>
 class RetainingSerializable : virtual public BaseClass {
  private:
   jsi::Runtime *primaryRuntime_;
-  jsi::Runtime *secondaryRuntime_;
+  jsi::Runtime *secondaryRuntime_ = nullptr;
   std::unique_ptr<jsi::Value> secondaryValue_;
 
  public:
@@ -57,15 +57,9 @@ class RetainingSerializable : virtual public BaseClass {
       // shared value is created and then accessed on the same runtime
       return BaseClass::toJSValue(rt);
     }
-    if (secondaryValue_ == nullptr) {
-      auto value = BaseClass::toJSValue(rt);
-      secondaryValue_ = std::make_unique<jsi::Value>(rt, value);
-      secondaryRuntime_ = &rt;
-      return value;
-    }
-    if (&rt == secondaryRuntime_) {
-      return jsi::Value(rt, *secondaryValue_);
-    }
+    // Never reuse a cached per-runtime jsi::Value: the original cached secondaryValue_ keyed by a raw
+    // secondaryRuntime_ pointer and cloned it on pointer match, a use-after-free when a destroyed runtime's
+    // address is reused (ABA) - it SIGSEGVs in ~jsi::Value before Nitro's own toObject cache-ABA surfaces.
     return BaseClass::toJSValue(rt);
   }
 
